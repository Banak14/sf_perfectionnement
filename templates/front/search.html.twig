{% extends "front/base.html.twig" %}

{% block body %}

	<h1>Les résultats de la recherche
		{{ term }}
		sont :</h1>

	{% for article in articles %}
		<h2>{{ article.title }}</h2>
        <h3>{{article.writer.name}}</h3>
	{% endfor %}

{% endblock %}