{% extends "admin/base.html.twig" %}

{% block body %}

	{% for category in categories %}

		<h2>
			<a href="{{ path('admin_category_show', {'id' : category.id }) }}">{{ category.name }}
			</a>
		</h2>


	{% endfor %}


{% endblock %}